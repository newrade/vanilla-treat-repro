import { createTheme } from 'treat';
import { Theme } from '@newrade/core-design-system';

const theme: Theme = {
  brandColor: 'blue',
  textColor: 'white',
};

export const treatTheme = createTheme(theme);
